name: scrumlab_snack
description: An extension library for easier access to commonly used functionality for SnackBars
version: 2.0.0
homepage: https://github.com/giulianojordao/scrumlab_snack

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  pedantic: ^1.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
